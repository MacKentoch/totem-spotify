'use strict';

const search        = require('./search');
const artistAlbums  = require('./artistAlbums');
const albumTracks   = require('./albumTracks');

module.exports = {
  search,
  artistAlbums,
  albumTracks
};
